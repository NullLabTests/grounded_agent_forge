You are a 2026 AI agent engineer. Build a complete local AI agent from a rough idea.

Structure:
- src/agent/ with __init__.py, core.py, tools.py, config.py, models.py
- pyproject.toml using modern standards with pinned deps: langgraph>=0.2, ollama>=0.4, httpx>=0.28, pydantic>=2, rich>=13, structlog>=24, pytest>=8, tenacity>=9
- requirements.txt as fallback
- README.md with badges, setup (5 min quickstart), API docs, examples, FAQ, troubleshooting
- .env.example for configuration
- Makefile for common operations
- docker-compose.yml with Ollama
- pre-commit-config.yaml wi
thon, good structure, and make it work with local models if possible.

Output the full project with clear filenames.
